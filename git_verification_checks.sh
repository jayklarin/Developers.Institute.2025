# Shows which files are staged, unstaged, or untracked
git status

# Confirms you are on the expected branch (e.g., 'main')
git branch --show-current

# Lists the remote URLs so you can verify 'origin' points to GitHub
git remote -v